namespace SunamoTextOutputGenerator.Tests;

public class TextOutputGeneratorStaticTests
{
    [Fact]
    public void CompareListTest()
    {
        var tog = new TextOutputGenerator();

        //var reposNames = new List<string>(["a", "c"]);
        //var l = new List<string>(["a", "d"]);

        //var both = CAG.CompareList(reposNames, l);

        //string s = TextOutputGeneratorStatic.CompareList(both, nameof(reposNames), reposNames, nameof(l), l);
    }
}